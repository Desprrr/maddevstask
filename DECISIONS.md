# Лог решений

Формат: `## YYYY-MM-DD HH:MM (TZ) — заголовок` + короткое описание: что решено/сделано, почему, что рассматривалось как альтернатива.
Записи добавляются сразу по принятии решения или после значимого шага, не задним числом в конце сессии.

## 2026-09-15 18:00 (+05:00) — Выбор сценария

Из 14 сценариев задания выбрано **Задание 3: Монитор доступности сайтов**.

Почему: по сравнению с другими сценариями (флэш-распродажа, шеринг транспорта, совместные расходы) здесь нет платежей и нет многопользовательских ролей с правами — меньше «бухгалтерской» бизнес-логики. При этом сценарий всё равно требует настоящей конкурентной инженерии: планировщик с независимыми интервалами на десятки целей, инцидент-детекция с порогом, real-time обновление у нескольких клиентов, окна обслуживания с подавлением алертов, история/агрегация без деградации на объёме — то есть есть что показать по существу, а не только CRUD-формы.

## 2026-09-15 18:05 (+05:00) — Стек

- Backend: **Python + FastAPI**. Обоснование: планировщик проверок — I/O-bound задача (сеть, ожидание ответа целевых сайтов), под это идеально ложится asyncio; Starlette (на котором стоит FastAPI) даёт нативный WebSocket без сторонних библиотек для real-time статуса.
- Frontend: **Vue 3 + TypeScript + Vite**.
- СУБД: **PostgreSQL** (SQLAlchemy async + asyncpg, миграции Alembic) — выбрана как предпочтительный вариант из требований задания.
- Выбор согласован с пользователем напрямую в диалоге (не мой единоличный выбор).

## 2026-09-15 18:07 (+05:00) — Модель доступа: без входа

Учётные записи — по заданию «на ваше усмотрение». Решено сделать инструмент single-tenant (один владелец мониторинга, без регистрации/логина). Публичная страница статуса — не второй тип пользователя, а read-only срез данных по флагу `is_public` на чеке/группе, без какой-либо авторизации.

Почему: регистрация и сессии не требуются логикой задания (в отличие от, например, сценария с ролями HR/руководитель) и добавили бы скоуп, не отражающий суть задачи — конкурентный мониторинг. Это будет явно объяснено в README как осознанное ограничение объёма, а не недосмотр.

## 2026-09-15 18:10 (+05:00) — Окружение разработки и локальный запуск

На машине для разработки нет Docker, нет установленного PostgreSQL и нет GitHub CLI (`gh`). Обнаружено при проверке инструментов (`docker`, `psql`, `gh` — все `command not found`), при этом доступны WSL2 с Ubuntu 26.04 (passwordless sudo) и Node v24 / Python 3.14 на самой Windows-машине.

Решение:
- Для **сдачи проекта** основной документированный способ запуска — `docker-compose up` (переносимо на машину рецензента, у которой Docker скорее всего есть).
- Для **проверки в процессе разработки** на этой машине — PostgreSQL, установленный через `apt` в WSL2, и backend/frontend, запущенные вручную (venv + npm) — этот путь будет реально протестирован и тоже задокументирован в README как альтернативный способ запуска.
- `docker-compose.yml`/Dockerfile будут написаны аккуратно, но не прогонялись через настоящий Docker в этой песочнице — это честно отмечается в README, а не выдаётся за протестированное.

## 2026-09-15 18:12 (+05:00) — GitHub-репозиторий

Пользователь создал пустой репозиторий: https://github.com/Desprrr/maddevstask — remote будет добавлен локально, push будет сделан только после явного подтверждения пользователем в конце работы (это публичное/сложно обратимое действие).

## 2026-09-15 18:13 (+05:00) — Планировщик: политика следующего запуска

Каждый check обслуживается одним `asyncio.Task`, работающим циклом `проба → запись результата → инцидент/алерт → WS-broadcast → sleep(interval) → повтор`. Следующий запуск планируется от **момента завершения предыдущей пробы**, а не по фиксированной сетке времени. Это даёт «проверка не запускается второй раз параллельно самой себе» автоматически (без отдельных локов), даже если проба длилась дольше своего интервала — единственная плата: интервал в этом случае временно «плывёт» (запуск позже расписания), что задокументировано как сознательный трейд-офф простоты против идеальной пунктуальности.

## 2026-09-15 18:13 (+05:00) — Порог инцидента

Кратковременный единичный сбой не считается падением. Порог: **2 подряд неуспешные проверки** — фиксированная константа на уровне приложения (не настраивается per-check в этой версии, чтобы не разрастался UI/скоуп там, где задание явно оставляет решение на нас). Названо в README как то, что можно вынести в настройку следующим заходом.

## 2026-09-15 18:22 (+05:00) — Проблема: жёстко закреплённые версии пакетов не собираются на Python 3.14

При первой установке зависимостей backend (`pip install -r requirements.txt`) с точно
зафиксированными версиями (`pydantic==2.10.4`, `asyncpg==0.30.0` и т.д.) сборка падала и на
Windows, и в WSL2 (Ubuntu 26.04, тоже Python 3.14) — для этих старых версий нет готовых wheel
под cp314, pip уходил в сборку из исходников (`pydantic-core` на Rust), а линковка падала
из-за не до конца настроенного MSVC/Rust тулчейна.

Исправление: в `requirements.txt` заменил точные версии на нижние границы (`>=`), чтобы pip
брал актуальные версии с готовыми wheel под используемый интерпретатор. После этого
`pip install` и `pytest` (health-check) прошли без проблем. Для сдающего проект это тоже
безопаснее: любой более новый/другой Python не упрётся в эту же стену.

## 2026-09-15 18:45 (+05:00) — Локальный PostgreSQL для разработки и проблема с SSL через WSL2

На машине нет Docker и нет нативного Postgres под Windows, зато есть WSL2 (Ubuntu 26.04,
passwordless sudo). Поставил PostgreSQL 18 через `apt` внутри WSL2, завёл роль/БД `monitor`.

Проблема: по умолчанию Postgres в WSL слушал только `listen_addresses=localhost` — недоступно
с Windows-стороны. Поправил на `listen_addresses='*'` и добавил в `pg_hba.conf` правило для
подсети WSL NAT (`172.16.0.0/12`, `scram-sha-256`), не открывая `0.0.0.0/0`. После этого
обычное TCP (проверено HTTP-сервером на порт) стало доступно с Windows по `localhost` и по IP
WSL, но именно Postgres-подключение (`asyncpg`) всё равно рвалось на этапе SSL-negotiation
(`connection_lost` при попытке SSL upgrade) — похоже на особенность NAT-трансляции
localhost-forwarding в этой связке WSL2/Windows, ломающую именно SSL-рукопожатие поверх
проброшенного порта.

Исправление: подключаться с явным `ssl=False` (локальный Postgres и так без TLS). В
`app/config.py` добавлен флаг `DATABASE_SSL` (по умолчанию `false`), передаётся в
`create_async_engine(..., connect_args={"ssl": settings.database_ssl})` — для прод/управляемой
БД с TLS переключается через переменную окружения, ничего не захардкожено.

Это же самое окружение (WSL2 Postgres) используется только для локальной разработки/тестов в
этой песочнице; для сдачи проекта основной путь — `docker-compose`, где Postgres поднимается
в контейнере и подобных проблем с NAT/SSL не будет в принципе.

## 2026-09-15 18:55 (+05:00) — Схема БД и первая миграция

Добавлены SQLAlchemy-модели (`app/models/`): `Group`, `GroupAlertEmail`, `Check`,
`CheckResult`, `Incident`, `MaintenanceWindow`, `SentEmail` — по модели из плана. Настроен
Alembic (`async` шаблон), `env.py` переписан так, чтобы URL и SSL-флаг брались из
`app.config.Settings` (единая точка правды, а не дублирование в `alembic.ini`).

Небольшие уточнения относительно плана, добавленные прямо в схему:
- `checks.interval_seconds` — CHECK-constraint `BETWEEN 30 AND 3600` прямо в БД (границы из
  задания), а не только валидация на уровне API.
- `maintenance_windows` — CHECK-constraint "ровно один из `check_id`/`group_id` заполнен",
  чтобы окно обслуживания не могло повиснуть без привязки или с двойной.
- `check_results` и `incidents` получили составные индексы `(check_id, ...at)` сразу, под
  требование "история за месяц открывается быстро".

Автосгенерированная миграция `initial_schema` проверена вручную (FK/ondelete, constraints,
индексы совпадают с моделями) и прогнана (`alembic upgrade head`) на локальном Postgres из
WSL2 — все 7 таблиц создались корректно.

Отдельно: WSL2-инстанс по умолчанию засыпает между вызовами (нет активного клиента) и
поднимает Postgres заново при следующем обращении — это ломало соединение с Windows-стороны
("Connection refused") просто из-за гонки. Держу фоновый `sleep infinity`-процесс в WSL на
время разработки, чтобы инстанс не завершался между командами. Это чисто dev-окружение этой
сессии и не имеет отношения к тому, как проект будет запускаться у проверяющего.

## 2026-09-15 19:05 (+05:00) — target-emulator

Отдельный маленький FastAPI-сервис `target-emulator/` с ручками `/ok`, `/status/{code}`,
`/delay/{ms}`, `/timeout` (спит час — дольше любого разумного timeout проверки, эмулирует
зависшую цель), `/flaky/{fail_rate}` (случайно отдаёт 500 с заданной вероятностью). Это и есть
"свой сервер, который умеет отвечать медленно/с ошибкой/не отвечать вовсе" из условия задания —
на нём дальше будут стоять демо-проверки, чтобы падение → инцидент → письмо → восстановление
можно было показать вживую, а не только в тестах на моках. Проверено юнит-тестами (TestClient)
и живым запуском (`uvicorn`, curl) на порту 9100.

## 2026-09-15 19:30 (+05:00) — CRUD API для групп и чеков

Добавлены роуты `/api/groups` и `/api/checks`: создание/список/получение/частичное
обновление/удаление, плюс `/api/checks/{id}/pause` и `/resume`. Валидация на уровне Pydantic
(диапазон `interval_seconds` 30..3600 и на схеме, и в БД-constraint — задание не просило,
но дешёвая защита от рассинхрона между API и БД), проверка существования `group_id` при
создании/переносе чека (иначе 404, а не силентный NULL).

`run-now` сознательно НЕ добавлен в этот же коммит, хотя в плане был в одном пункте с CRUD:
он по смыслу требует общей с планировщиком логики "выполнить пробу и записать результат", а
планировщика ещё нет. Решил не городить временную реализацию, которую потом придётся
переписывать — `run-now` появится вместе с планировщиком следующим шагом, на той же функции
проверки, что использует и фоновый цикл. Небольшое уточнение порядка шагов из плана, не меняет
итоговый объём.

Тесты (`pytest-asyncio`, `httpx.AsyncClient` + `ASGITransport`, реальный Postgres —
отдельная БД `monitor_test`, схема создаётся/дропается на сессию, таблицы чистятся после
каждого теста) — 13 тестов на CRUD, валидацию и краевые случаи (несуществующая группа,
удаление группы разгруппировывает её чеки, частичный PATCH не трогает остальные поля).

Проблема: `email-validator` не был явно указан в зависимостях, хотя `pydantic.EmailStr`
использован в схеме группы — упало на импорте. Добавлено `pydantic[email]` в
`requirements.txt`.

Проблема: первый прогон тестов падал с `InterfaceError: cannot perform operation: another
operation is in progress` почти на каждом обращении к БД. Причина — дефолтный пул соединений
`create_async_engine` держит asyncpg-соединения между вызовами, а `pytest-asyncio` даёт
каждому тесту собственный event loop; переиспользование соединения, открытого в одном loop'е,
из другого — и есть эта ошибка. Исправление: тестовый engine создаётся с `poolclass=NullPool`
(каждый запрос открывает и закрывает соединение заново, между loop'ами ничего не шарится).
Прод/дев engine (`app/db.py`) это не трогает — там пул нужен и полезен, там один живой loop.

## 2026-09-15 19:55 (+05:00) — Планировщик проверок и run-now

Реализован `app/scheduler/`: `prober.py` (одна HTTP-проба через `httpx.AsyncClient`, никогда не
бросает исключение наружу — таймаут/сетевая ошибка превращаются в `ProbeOutcome(success=False,
error=...)`) и `engine.py` (`Scheduler` — по одному `asyncio.Task` на активный чек, см. решение
от 18:13 про политику "следующий запуск от момента завершения").

`run-now` вынесен из CRUD-коммита сюда, как и планировалось решением от 19:30 — реализован как
`Scheduler.trigger_now(check_id)`: если чек уже в расписании — выставляет `asyncio.Event`,
которым будит цикл, и ждёт `run_done`; если чек на паузе — делает разовую пробу напрямую, под
`asyncio.Lock` на check_id (чтобы двойной клик по "запустить сейчас" на приостановленном чеке
тоже не привёл к двум параллельным пробам).

Найден и исправлен баг конкурентности до того, как он попал в закоммиченный код: изначально
`trigger.clear()` стоял ПОСЛЕ пробы, из-за чего запрос "run now", выставленный, пока проба уже
выполняется, молча терялся — чек просидел бы без реакции до конца полного interval. Перенёс
`trigger.clear()` в начало цикла, до пробы: теперь `trigger`, выставленный в любой момент,
гарантированно вызывает новый прогон не позже чем сразу после уже идущей пробы. Обнаружено и
исправлено именно через тесты (`tests/test_scheduler.py`) на фейковой пробе с управляемой
задержкой — реальный сетевой вызов для такой проверки конкурентности не нужен и был бы
источником нестабильности тестов.

Тесты планировщика (`ProbeSpy` — фейк с управляемой задержкой и счётчиком параллельности вместо
реальных HTTP-запросов, чтобы тесты были быстрыми, детерминированными и не зависели от сети):
первая проба стартует сразу после `add()`; `run-now` после завершения цикла запускает новую
пробу; `run-now`, застигнутый ровно во время уже идущей пробы, не создаёт параллельного вызова
и не теряется — следующая проба всё равно происходит; `run-now` на приостановленном чеке
отрабатывает разово; двойной `run-now` на приостановленном чеке сериализуется локом;
`remove()` останавливает дальнейшие пробы. 19 тестов, прогнано 3 раза подряд без нестабильности.

Дополнительно — живая end-to-end проверка (не только моки): поднял backend (`uvicorn`,
dev-Postgres) и `target-emulator` (`uvicorn`, порт 9100) и через `curl` прогнал реальные
сценарии — успешная проба (`/ok`), проба с неверным статусом (`/status/500`, `error: "status
500 != expected 200"`), проба с реальным таймаутом (`/timeout` при `timeout_ms=500` — ответ за
~0.67s с `error: "timeout"`), и `run-now` на активном и на приостановленном чеке через API.
Всё отработало как задумано. Тестовые данные из dev-БД после проверки вычищены (`TRUNCATE`).
