# Скрипты, которые запускаются внутри Linux-контейнеров, обязаны иметь LF
# независимо от платформы, на которой их чекаутят (иначе на Windows с
# core.autocrlf=true шебанг "#!/bin/sh\r" ломает запуск в контейнере).
*.sh text eol=lf
docker-entrypoint.sh text eol=lf
